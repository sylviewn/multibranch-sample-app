pipeline {
  agent any
    stages {
       stage('Hello') {
         steps {
           echo " hello world"
          }
       }
       stage('README')
         when{
            branch "feature1"
          }
         steps{
             echo "README.md"  
         }
    }
 }
